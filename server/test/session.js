"use strict";

var util = require("util");
util.log("lib session tests");

describe("lib session", function () {
  it("create a new session for an account and return token");
  it("lookup and return a given session by token");
  it("mark a session invalid");
  it("invalidate all sessions for an account invalid");
  it("invalidate all sessions");
});
